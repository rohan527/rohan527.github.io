import Typed from 'typed.js';

var options = {
  strings: ["I'm a Master's student at UCSC", 'I work on ML and AI projects'],
  typeSpeed: 120,
  backSpeed: 50,
  backDelay: 1500
  // loop: true
};

var typed = new Typed('#headline', options);
